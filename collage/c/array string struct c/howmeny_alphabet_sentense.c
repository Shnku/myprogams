#include <stdio.h>
int *no_of_alpha(char *);

int main()
{
    char str[50];
    printf("\nenter the sentense: ");
    fgets(str, 50, stdin);

    return 0;
}

int *no_of_alpha(char *sen)
{
    int *arr, i = 0;
    while (*sen)
    {
        
        sen++;
    }
}
