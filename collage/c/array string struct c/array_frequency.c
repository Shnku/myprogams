/*array frequency means which character have each time....*/
#include <stdio.h>
#include "get_put_array.h"
#define length 5

int *store_frequency(int *a, int len);

int main()
{
    int a[length];
    get_array_data(a, length);
    return 0;
}

int *store_frequency(int *a, int len)
{
    
}
